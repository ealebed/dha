version: 2
updates:
  - package-ecosystem: "gomod"
    directory: "/"
    schedule:
      interval: "daily"
    # Optional: Add commit message prefix for Go module updates
    # commit-message:
    #   prefix: "deps"
    #   include: "scope"

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "daily"
    # Optional: Add commit message prefix for GitHub Actions updates
    # commit-message:
    #   prefix: "ci"
    #   include: "scope"

  # Optional: Uncomment if your project uses Docker
  # - package-ecosystem: "docker"
  #   directory: "/"
  #   schedule:
  #     interval: "daily"

  # Optional: Uncomment if your project uses Terraform modules
  # - package-ecosystem: "terraform"
  #   directory: "/"
  #   schedule:
  #     interval: "daily"
